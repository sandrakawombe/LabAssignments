spring.application.name=BookClient

# The client runs on a different port than the BookApplication (8080)
server.port=8081

# Base URL of the BookApplication REST interface
book.service.url=http://localhost:8080/books
